// trivial test demonstrating the proper successful return value
// note that we explicitly quit if at the end of the test, b != 1
// this allows tests to be run by other tests with a different
// overall hoc environment
b = 1
if (b != 1) { quit() }

